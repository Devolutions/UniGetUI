<?xml version="1.0" encoding="utf-8" ?>
<Window
  x:Class="UniGetUI.Interface.MainWindow"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:animations="using:CommunityToolkit.WinUI.Animations"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:widgets="using:UniGetUI.Interface.Widgets"
  Title="UniGetUI"
  mc:Ignorable="d"
>
  <Window.SystemBackdrop>
    <MicaBackdrop Kind="Base" />
  </Window.SystemBackdrop>
  <Grid x:Name="MainContentGrid" x:FieldModifier="public" Visibility="Visible">
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <TitleBar
      x:Name="TitleBar"
      Title="UniGetUI"
      Grid.Row="0"
      Margin="0,0,0,-4"
      HorizontalContentAlignment="Center"
      BackRequested="TitleBar_OnBackRequested"
      IsBackButtonVisible="False"
      IsPaneToggleButtonVisible="True"
      PaneToggleRequested="TitleBar_PaneToggleRequested"
      SizeChanged="TitleBar_SizeChanged"
      Visibility="Collapsed"
      IsTabStop="False"
    >
      <!--TitleBar.IconSource>
                <ImageIconSource ImageSource="ms-appx:///Assets/Images/icon.png" />
            </-TitleBar.IconSource-->
      <TitleBar.Content>
        <AutoSuggestBox
          x:Name="GlobalSearchBox"
          Width="400"
          Height="32"
          BorderBrush="{ThemeResource ButtonBorderBrush}"
          HorizontalAlignment="Stretch"
          x:FieldModifier="public"
          QueryIcon="Find"
        />
      </TitleBar.Content>
    </TitleBar>
    <StackPanel Grid.Row="1" Margin="0" Padding="0" BorderThickness="0" Spacing="0">
      <InfoBar
        Name="UpdatesBanner"
        Margin="0,0,0,4"
        x:FieldModifier="public"
        BorderThickness="0,1,0,1"
        CornerRadius="0"
        IsOpen="False"
        Visibility="{x:Bind UpdatesBanner.IsOpen, Mode=OneWay}"
      />
      <InfoBar
        Name="ErrorBanner"
        Margin="0,0,0,4"
        x:FieldModifier="public"
        BorderThickness="0,1,0,1"
        CornerRadius="0"
        IsOpen="False"
        Severity="Error"
        Visibility="{x:Bind ErrorBanner.IsOpen, Mode=OneWay}"
      />
      <InfoBar
        Name="WinGetWarningBanner"
        Margin="0,0,0,4"
        x:FieldModifier="public"
        BorderThickness="0,1,0,1"
        CornerRadius="0"
        IsOpen="False"
        Severity="Warning"
        Visibility="{x:Bind WinGetWarningBanner.IsOpen, Mode=OneWay}"
      />
      <InfoBar
        Name="TelemetryWarner"
        Margin="0,0,0,4"
        x:FieldModifier="public"
        BorderThickness="0,1,0,1"
        CornerRadius="0"
        IsOpen="False"
        Severity="Informational"
        Visibility="Collapsed"
      />
    </StackPanel>
    <Frame Name="MainContentFrame" Grid.Row="2">
      <Grid Background="{StaticResource ProgressBarBorderThemeBrush}" Visibility="Visible" AutomationProperties.Name="Loading UniGetUI">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
          <RowDefinition Height="*" />
          <RowDefinition Height="Auto" />
          <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Canvas
          x:Name="LaunchBackground"
          Grid.Row="0"
          Grid.RowSpan="5"
          Grid.Column="0"
          Grid.ColumnSpan="3"
          Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
        />
        <!-- Splash: column/row ratios match the wordmark bounding box (x 245-480, y 78-193) in the 620x300 source SVG so the tagline cell lands directly below the wordmark. -->
        <Grid Grid.Row="1" Grid.Column="1" Width="540">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="245*" />
            <ColumnDefinition Width="235*" />
            <ColumnDefinition Width="140*" />
          </Grid.ColumnDefinitions>
          <Grid.RowDefinitions>
            <RowDefinition Height="200*" />
            <RowDefinition Height="100*" />
          </Grid.RowDefinitions>
          <Image Grid.ColumnSpan="3" Grid.RowSpan="2" Stretch="Uniform">
            <animations:Explicit.Animations>
              <animations:AnimationSet x:Name="InAnimation_Border">
                <animations:TranslationAnimation
                  EasingMode="EaseOut"
                  From="0,40"
                  To="0,0"
                  Duration="0:0:0.7"
                />
                <animations:OpacityAnimation
                  EasingMode="EaseOut"
                  From="0"
                  To="1"
                  Duration="0:0:0.7"
                />
              </animations:AnimationSet>
            </animations:Explicit.Animations>
            <Image.Source>
              <BitmapImage UriSource="ms-appx:///Assets/SplashScreen.png" />
            </Image.Source>
          </Image>
          <widgets:TranslatedTextBlock
            Grid.Column="1"
            Grid.Row="1"
            HorizontalAlignment="Center"
            VerticalAlignment="Top"
            Margin="0,8,0,0"
            FontFamily="Segoe UI Variable"
            FontSize="15"
            Text="Package management made easy"
          >
            <animations:Explicit.Animations>
              <animations:AnimationSet x:Name="InAnimation_Text">
                <animations:OpacityAnimation
                  EasingMode="EaseOut"
                  From="0"
                  To="1"
                  Duration="0:0:0.7"
                />
              </animations:AnimationSet>
            </animations:Explicit.Animations>
          </widgets:TranslatedTextBlock>
        </Grid>
        <ProgressRing
          Name="LoadingIndicator"
          Grid.Row="1"
          Grid.Column="1"
          Margin="0,0,0,-48"
          VerticalAlignment="Bottom"
          HorizontalAlignment="Center"
          Foreground="#08a9c3"
          IsIndeterminate="True"
          Visibility="Visible"
        />
      </Grid>
    </Frame>

    <TeachingTip
      x:Name="DismissableNotification"
      x:FieldModifier="public"
      PlacementMargin="20"
      PreferredPlacement="Auto"
    />
  </Grid>
</Window>
